{
  "name": "wbitencourt",
  "version": "1.0.44",
  "description": "",
  "main": "dist/index.js",
  "bin": {
    "wbitencourt": "dist/index.js"
  },
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rm -rf dist && tsc && node --input-type=module -e \"import fs from 'fs-extra'; await fs.copy('../util/src', 'dist/templates', { filter: (src) => !src.endsWith('.spec.ts') });\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "@types/fs-extra": "^11.0.4"
  },
  "dependencies": {
    "chalk": "^5.4.1",
    "fs-extra": "^11.3.0"
  }
}
