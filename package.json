{
  "name": "wbitencourt",
  "version": "1.0.0",
  "description": "npm package with reusable utilities by Wendell Bitencourt",
  "workspaces": [
    "packages/**"
  ],
  "keywords": [
    "npm",
    "package",
    "wbitencourt"
  ],
  "homepage": "https://github.com/WBitencourt/npm-wbitencourt#readme",
  "bugs": {
    "url": "https://github.com/WBitencourt/npm-wbitencourt/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/WBitencourt/npm-wbitencourt.git"
  },
  "license": "ISC",
  "author": "Wendell Bitencourt",
  "type": "module",
  "scripts": {
    "test": "npx vitest run",
    "build": "npm run build --workspaces",
    "prepare": "npm run build",
    "pack:util": "npm --prefix packages/util run build && cd packages/util && npm pack --dry-run && cd ../..",
    "pack:cli": "npm --prefix packages/cli run build && cd packages/cli && npm pack --dry-run && cd ../..",
    "deploy": "./deploy.sh"
  }
}
